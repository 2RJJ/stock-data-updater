name: Yahoo Finance → /Users/ronjones/Documents/YahooFinance
on:
  schedule:
    - cron: '0 0 * * 1-5'                  # Daily full history
    - cron: '14,29,44,59 13-20 * * 1-5'    # Every 15 min market hours
  workflow_dispatch:
    inputs:
      mode:
        description: 'Run mode'
        required: true
        default: '15min'
        type: choice
        options: ['daily', '15min']
